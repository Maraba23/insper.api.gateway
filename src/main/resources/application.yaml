spring:
  application:
    name: api-gateway

  cloud:
    discovery:
      locator:
        enabled: true
    gateway:
      routes:
        - id: ingrediente
          uri: lb://ingrediente
          predicates:
            - Path=/ingrediente/**

        - id: api-receitas
          uri: lb://api-receitas
          predicates:
            - Path=/api-receitas/**

        - id: api-auth
          uri: lb//api-auth
          predicates:
            - Path=/api-auth/**

eureka:
  client:
    register-with-eureka: true
    fetch-registry: true
    service-url:
      defaultZone: ${EUREKA_URI:http://localhost:8761/eureka/}

api:
  endpoints:
    open: >
        ANY /auth/register,
        POST /auth/login
